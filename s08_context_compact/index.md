# s08 · Context Compact — 上下文总会满，要有办法腾地方

> *"最近的信息保持原样，越早的信息压得越狠。"*

## 本章要解决的问题

`messages[]` 只会增长，不会缩小。长会话迟早撞上模型的上下文窗口：轻则报 `invalid_request_error`，重则模型在超长上下文里"迷路"。

Harness 必须主动管理上下文容量。本章实现一条**四级递进的压缩管线**，每轮 API 调用前自动体检：

```
    每轮调用前估算 token 用量
        |
    < TOKEN_BUDGET? ----------------> 不处理，直接调用
        | 超了
    级别1 snip:     截断历史中过大的 tool_result（便宜、无损于结构）
        | 还超
    级别2 micro:    早期消息替换成占位文本，只保最近 N 条（零成本）
        | 逼近硬预算
    级别3 summary:  让 LLM 把早期历史浓缩成摘要（最贵，但保信息）
        |
    压缩后的 messages[] 进入本轮调用
```

## 三条设计原则

1. **最近的对话永远不动。** 模型正在依赖最近几轮做决策，压缩它们等于让 agent 失忆。代码里所有级别都遵守 `RECENT_KEEP` 边界。
2. **越早的信息压得越狠。** 时间越久，细节越不重要。
3. **分级递进，先用便宜手段。** snip 只是字符串截断；micro 是零成本替换；只有最后才动用 LLM 摘要（要花一次 API 调用）。

## 摘要怎么写

级别3 让模型当"压缩器"，prompt 里明确了**保留什么、丢掉什么**：

```
必须保留：用户目标、已完成的工作、关键事实与文件路径、未尽事项。
丢掉：中间过程、重复尝试、工具输出细节。
```

摘要替换掉早期消息后，对话就能无缝继续——模型看到的第一条消息是"[harness: 以下是早期对话的摘要]"。

## 一个工程细节：消息形态规范化

Anthropic SDK 返回的 `response.content` 是对象列表，而压缩函数需要操作 dict。所以每轮结束后调 `normalize_history`，把历史统一转成 dict 结构，后续的截断、替换才能安全进行。

## 试一试

代码里故意把 `TOKEN_BUDGET` 调小（20000，约合 6 万字符），方便观察：

```sh
python s08_context_compact/code.py
```

连续让它读几个大文件，几轮后你会看到紫色的 `[compact] 级别1 snip 完成 -> xxx tokens` 日志。把预算再调小可触发摘要压缩。

## 动手练习

1. 把 `estimate_tokens` 换成真实 tokenizer（如用 `tiktoken`），对比估算误差。
2. 在级别3 摘要时，把当前的 todo 列表（参考 s05）一并附给压缩器，保证"进度"不会在压缩中丢失。
3. 思考：为什么 micro 压缩要放在 summary 之前，而不是之后？

## 下一章

[s09 Memory](../s09_memory/) — 记住该记的，忘掉该忘的。
